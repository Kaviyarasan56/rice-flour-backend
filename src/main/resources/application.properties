# Use Railway port
server.port=${PORT:8080}

# MySQL database connection
spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQL_DATABASE}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}

# # JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

 # Optional: explicitly set Hibernate dialect (Spring Boot can auto-detect)
 spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect








# # Server
# server.port=8080

# # Datasource (local MySQL). Replace username/password/db with Railway values when deploying.
# spring.datasource.url=jdbc:mysql://localhost:3306/rice_flour_shop?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=kavikavi56

# # Hibernate / JPA - auto update schema
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=false
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# # Logging (optional)
# logging.level.org.hibernate.SQL=DEBUG
